/**
 * joke service
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::joke.joke');

// create a getAlreadyVoted function that returns the ids of the jokes that the user or ip has already voted on 


// Function to get the IDs of jokes that the user or IP has already voted on

// Function to count pending jokes that the user or ip has not voted on

